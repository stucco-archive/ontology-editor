Ontology Modifier
=================

Simple ontology modification through JSON and Node-link diagrams.

Usage
=====

    npm install -d && bower install
    grunt server

Push to gh-pages
----

    grunt
    grunt gh
